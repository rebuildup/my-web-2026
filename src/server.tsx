/**
 * Empty placeholder.
 *
 * `@tanstack/react-start/server-entry` is resolved virtually by the
 * `@tanstack/react-start/plugin/vite` plugin and emits the Cloudflare Worker
 * fetch handler from the routes + start configuration below.
 *
 * If a future use case truly needs to compose a custom handler (e.g. add a
 * non-TanStack global middleware), this file can be replaced and `wrangler.jsonc`
 * `main` set to `./src/server.tsx`.
 */
export {};
